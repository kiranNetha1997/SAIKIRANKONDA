import { Application } from 'express';
export interface AuthRouteOptions {
    loginURL?: string;
    callbackURL?: string;
    afterLogin?: string;
    client_id: string;
    client_secret: string;
}
export declare function registerAuthRoutes(app: Application, options: AuthRouteOptions): void;
